# Chapter 1. Computer Networks and the Internet
## 1.4 Delay, Loss, and Throughput in Packet-Switched Networks
- 현실에서 데이터 손실 없이 두 단말 시스템 간 무제한 데이터 즉시 전송은 불가능
- 패킷이 한 노드(호스트 또는 라우터)에서 다음 노드(호스트 또는 라우터)로 이동 시 발생하는 지연  
  - 노드 처리 지연+큐잉 지연+전송 지연+전파 지연 = 총 노드 지연
- 패킷이 상위 노드에서 라우터 A의 라우터 B로 연결되는 아웃바운드 링크를 거쳐 라우터 B로 전송 
- 링크 앞에는 큐(버퍼라고도 함) 존재
- 패킷은 다음 조건에서만 링크로 전송 가능
  - 현재 링크에서 다른 패킷이 전송 중이지 않을 때
  - 큐에 앞서 대기 중인 다른 패킷이 없을 때
- 링크에 이미 대기 중인 패킷이 있으면 새로 도착한 패킷은 큐에 추가
### 지연의 종류

1. **처리 지연 (Processing Delay)**  
  - 패킷 헤더를 검사하고 패킷의 전송 방향을 결정하는 데 필요한 시간  
  - 일반적으로 마이크로초(μs) 이하 수준  

2. **큐잉 지연 (Queuing Delay)**  
  - 패킷이 큐에서 링크로 전송되기를 기다리는 동안 발생하는 지연  
  - 트래픽이 많고 다른 패킷들이 대기 중일 경우 큐잉 지연이 길어짐  
  - 실제로는 마이크로초(μs)에서 밀리초(ms) 수준  

3. **전송 지연 (Transmission Delay)**  
  - 패킷은 선입선출(FIFO) 방식으로 전송됨  
  - 앞서 도착한 모든 패킷이 전송된 후에야 해당 패킷 전송 가능  
  - 전송 지연은 L/R (패킷 길이/링크 전송률)로 계산  
  - 일반적으로 마이크로초(μs)에서 밀리초(ms) 수준  

4. **전파 지연 (Propagation Delay)**  
  - 패킷이 링크의 시작점에서 라우터 B까지 전파되는 데 걸리는 시간  
  - 링크의 물리적 매체에 따라 달라짐  
  - 일반적으로 밀리초(ms) 수준
#### 전송지연과 전파지연의 차이
  - **전송 지연**: 라우터가 패킷을 내보내는 데 필요한 시간 vs  **전파 지연**: 비트가 한 라우터에서 다음 라우터로 전파되는 데 걸리는 시간
    
    ex)톨게이트 사이의 고속도로 구간을 링크로, 톨게이트를 라우터로 간주
    - 톨게이트를 빠져나갈 때 생기는 지연: 전송지연
    - 톨게이트에서 다음 톨게이트까지 걸리는 시간: 전파지연
### 큐잉 지연과 패킷 손실  
-10개의 패킷이 빈 큐에 동시에 도착  
  - 첫 번째 패킷은 큐잉 지연 없음  
  - 마지막 패킷은 다른 9개 패킷 전송을 기다리며 큰 큐잉 지연 발생 
- **트래픽 강도 (Traffic Intensity)**  
  - a: 패킷이 큐에 도착하는 평균 속도  
  - R: 전송 속도  
  - 모든 패킷은 L 비트로 구성  
  - 트래픽 강도 = La/R  
  - La/R > 1: 비트 도착 속도> 전송 속도 → 큐 길이 무한 증가, 큐잉 지연 무한대 접근= 패킷 손실가능성↑
  - La/R < 1: 전송 속도> 비트 도착 속도 → 패킷 손실x
  - 시스템 설계: La/R ≤ 1 유지해야 함
- **큐 도착 패턴**  
  - 패킷 도착은 일반적으로 무작위 (패턴 없음, 시간 간격 무작위)  
  - 트래픽 강도가 1에 가까워질수록 평균 큐 길이 크게 증가  
  - 강도가 약간 증가해도 지연은 훨씬 큰 비율로 증가  
- **패킷 손실 (Packet Loss)**  
  - 큐에 저장 공간 없으면 라우터는 패킷 드롭 → 패킷 손실  
  - 노드 성능은 지연뿐 아니라 패킷 손실 확률로도 측정    
- **종단간 지연 (End-to-End Delay)**  
  - d<sub>end−end</sub> = N(d<sub>proc</sub> + d<sub>trans</sub> + d<sub>prop</sub>)  (N: 링크 수, <sub>proc</sub>: 처리 지연, d<sub>trans</sub>: 전송 지연, d<sub>prop</sub>: 전파 지연)
- **종단간 처리량 (End-to-End Throughput)**  
  - 순간 처리량: 수신자가 파일을 수신하는 순간 속도 (비트/초)  
  - 평균 처리량 = F/T 비트/초 (파일 크기 F 비트, 전송 시간 T초)  
  - F 비트 파일 전송 시간: F/**min{R<sub>s</sub>, R<sub>c</sub>} (병목 링크의 전송 속도)** 
    - 서버-라우터 링크 속도: R<sub>s</sub>, 라우터-클라이언트 링크 속도: R<sub>c</sub>      
    - 저장-전달, 처리 지연, 프로토콜 문제는 고려하지 않음  
  - **n 링크 네트워크**(서버-클라이언트 간 n개 링크)
    - 처리량: min{R<sub>1</sub>, R<sub>2</sub>, …, R<sub>n</sub>} (경로 상 최소 전송 속도)  
    - 처리량은 데이터 흐름 경로의 링크 전송 속도 및 중간 트래픽에 의존
      ->높은 전송 속도 링크라도 다른 데이터 흐름이 많으면 병목 링크 될 수 있음
