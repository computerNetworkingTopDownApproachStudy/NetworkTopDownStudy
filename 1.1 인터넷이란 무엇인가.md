# Chapter 1. Computer Networks and the Internet
## 1.1 What is the Internet?
호스트(host, end system): 인터넷에 연결되는 장치
- 한 호스트가 다른 호스트로 데이터를 보낼 때 호스트는 데이터를 분할하고 각 분할된 데이터에 헤더 바이트를 추가한다.
  이렇게 생성된 정보 패키지는 컴퓨터 네트워크 용어로 패킷이라고 불리며,네트워크를 통해 다른 호스트에서 전송된 후 원래 데이터로 재조립된다.
- 인터넷 서비스 제공업체(ISP)를 통해 인터넷에 접속
  
패킷 교환 네트워크: 송신 엔드 시스템->통신 링크->패킷스위치->수신 엔드 시스템
- 패킷 스위치: 패킷을 목적지로 전달
  - 링크 계층 스위치: 액세스 네트워크
  - 라우터: 네트워크 코어

인터넷 서비스 제공업체(ISP)
- 하위 계층 ISP: 지역적 또는 특정 사용자 그룹에 초점을 맞춘 네트워크->개인 대상
- 상위 계층 ISP: 하위 계층 ISP들을 상호 연결->ISP 대상

엔드 시스템, 패킷 스위치, 그리고 인터넷의 다른 구성 요소들은 인터넷 내에서 정보의 송수신을 제어하는 프로토콜 = TCP/IP 프로토콜

## 1.2 A Services Description
애플리케이션에 서비스를 제공하는 인프라로서의 인터넷

분산 애플리케이션(distributed application): 여러 엔드 시스템을 포함한 애플리케이션
- 한 엔드 시스템에서 실행되는 프로그램이 인터넷을 통해 다른 엔드 시스템에서 실행되는 특정 프로그램으로 데이터를 전송
- 인터넷에 연결된 엔드 시스템이 데이터를 전송할 때 따라야 할 규칙 집합 = **소켓 인터페이스**

## 1.3 What is a Protocol?
프로토콜
- 작업을 수행하려면 동일한 프로토콜을 실행하는 둘 이상의 통신 주체가 필요
- 둘 이상의 통신 주체 간에 교환되는 메세지의 형식과 순서, 그리고 메시지의 송신 또는 수신 또는 기타 이벤트 발생 시 취해지는 행동을 정의
