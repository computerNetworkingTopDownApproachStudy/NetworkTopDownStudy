# Chapter 1. Computer Networks and the Internet
## 1.3 The Network Core
### 패킷 스위칭
- 메시지: 소스에서 목적지로 전송
- 긴 메시지 → 패킷으로 분할
- 패킷 이동 경로: 통신 링크 + 패킷 스위치 (라우터, 링크 레이어 스위치)
- 전송 속도: 링크의 전체 전송 속도
- 패킷 크기 L 비트, 링크 전송 속도 R 비트/초 → 전송 시간 L/R 초
#### Store-Forward 전송
- 대부분의 패킷 스위치는 저장 후 전달(Store-and-Forward) 방식 사용
- 소스가 패킷 1의 일부를 전송, 패킷 1의 앞부분이 라우터에 도착 -> 저장 후 전달 방식 때문에 라우터는 받은 비트를 즉시 전송 불가(라우터는 패킷의 모든 비트를 버퍼에 **저장(Store)**)
-> 모든 비트를 받은 후에야 패킷을 출력 링크로 **전송(Forward)** 시작
#### Queuing 지연, 패킷 손실
- 패킷 스위치: 여러 링크 보유
- 출력 버퍼(출력 큐): 라우터가 링크로 보낼 패킷 저장
  
  - 링크가 다른 패킷 전송으로 바쁘면, 도착한 패킷은 출력 버퍼에서 대기 -> 큐잉 지연(Queuing Delays) 발생
  - 도착 패킷이 버퍼가 다른 패킷들로 가득 찬 경우 발견 -> 패킷 손실(Packet Loss) 발생
#### Forwarding 테이블과 라우트 프로토콜
- 라우터: 입력 통신 링크로 도착한 패킷을 다른 출력 통신 링크로 전달
- 방법:
  1. 인터넷에서 모든 엔드 시스템은 **IP 주소** 보유
  2. 소스는 패킷 헤더에 목적지의 **IP 주소** 포함
  3. 각 라우터는 **포워딩 테이블(Forwarding Table)** 보유
  4. 패킷이 라우터에 도착하면, 라우터는 포워딩 테이블을 참조하여 적절한 **출력 링크** 결정
- **종단 간 라우팅 과정**: 패킷이 목적지까지 이동하는 과정 = "물어물어서 길 찾는 사람"
- 인터넷의 **라우팅 프로토콜**(포워딩 테이블 자동 설정)
### 회선 스위칭과 패킷 스위칭
네트워크를 통해 데이터를 이동시키는 두 가지 기본 방식
- **회선 스위칭(Circuit Switching)**
  - 엔드 시스템 간 통신을 위해 경로상의 자원(버퍼, 링크 전송 속도)을 통신 세션 동안 **예약**
  - ex) 전통적인 전화 네트워크
    -  발신자와 수신자 간 경로의 스위치가 **연결 상태 유지**
    - 예약된 자원은 **고정된 일정 속도**로 보장
  - **자원 할당**: 수요와 관계없이 링크 사전 예약.
      
  <-> **자원 낭비**: 침묵 기간 동안 전용 회선 유휴.
    
  <-> **복잡성**: 종단 간 회선 설정 및 전송 용량 예약에 복잡한 시그널링 소프트웨어 필요.
- **패킷 스위칭(Packet Switching)** 인터넷과 같은 네트워크
  - 패킷은 링크 자원을 예약하지 않고 네트워크로 전송
  - ex) 인터넷
  - 전송 링크의 송신 측에서 버퍼에 대기하며 **지연** 발생
  - 품질 보장 없음
  - **자원 할당**: 링크 사용을 **수요 기반**으로 동적 할당.
    
  <-> **실시간 서비스 부적합**: 가변적/예측 불가능한 종단 간 지연(주로 큐잉 지연 때문).
  - 회선 스위칭보다 전송 용량 공유 우수, 구현이 간단, 효율적, 비용 저렴. -> 현재 트렌드
#### 회선 스위칭의 데이터 전송
- **다중화**: 여러 신호를 하나의 전송 경로를 통해 동시에 전송하기 위해 자원(주파수 또는 시간)을 나누는 기술
  - FDM(주파수분할다중화):
    - 주파수 도메인을 n개의 대역으로 분할
    - 각 신호는 서로 다른 주파수 대역 사용
  - TDM(시간분할다중화):
    - 시간 도메인을 프레임으로 분할
    - 각 신호는 특정 시간 슬롯에서 전송
### 네트워크 연결
- **인터넷 접속 ISP 상호 연결**: 모든 접속 ISP는 상호 연결 필요.
- 네트워크 구조
  
  - **네트워크 구조 1**
    - 단일 **글로벌 트랜싯 ISP**가 모든 접속 ISP 연결.      
  - **네트워크 구조 2**
    - 수십만 개의 접속 ISP와 여러 **글로벌 트랜싯 ISP**.
    - 글로벌 트랜싯 ISP 간 상호 연결.
    - **2계층 구조**: 지역 ISP → **1계층 ISP** 연결.
      - 비용: 접속 ISP는 지역 ISP에, 지역 ISP는 1계층 ISP에 지불.
  - **네트워크 구조 3**
    -  **다계층 구조**: 도시별 접속 ISP → 주 ISP → 국가 ISP → 1계층 ISP.
  - **네트워크 구조 4**
    - **PoP**: 최하위(접속 ISP) 제외 모든 계층- 고객 ISP가 제공자의 ISP에 연결할 수 있도록 제공자 ISP의 PoP(라우터 그룹) 존재.
    - **멀티호밍**: 1계층 ISP 제외 모든 ISP- 2개 이상 제공자 ISP에 연결 가능.
    - **피어링(Peering)**: 고객 ISP는 글로벌 인터넷 연결 위해 제공자 ISP에 비용 지불 필요 -> 동일 계층의 인접 ISP 간 비용 절감을 위해 직접 연결.
    - **IXP(인터넷 교환 지점)**: 제3자 회사가 여러 ISP 피어링을 위한 연결 지점 제공.
  - **네트워크 구조 5(오늘날 인터넷)**:
    - **콘텐츠 제공자 네트워크** 추가(예: 구글).
      - 구글 전용 네트워크: 구글 서버로/로부터의 트래픽만 처리 -> 상위 계층 우회(하위 ISP와 **무료 피어링**(직접 연결 또는 IXP 통해)).
![image](https://github.com/user-attachments/assets/a44746ce-2d2c-40f1-9bdc-2114c8cdc981)

   
  

